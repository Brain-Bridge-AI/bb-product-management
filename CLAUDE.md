# bb-product-management

> **Governed by [The Library](https://github.com/Brain-Bridge-AI/the-library).** For catalog-managed installs, install the catalog skill (`mkdir -p ~/.claude/skills && git clone https://github.com/Brain-Bridge-AI/the-library.git ~/.claude/skills/library`), then pull skills with `/library use <name>`.
